/**
 * U can have more files in folder like:
 * - Some summary of a page
 * - The first component (ex: a table with data)
 * - The second component (ex: the data from each cell from table when the cell is clicked)
 * ...
 */
